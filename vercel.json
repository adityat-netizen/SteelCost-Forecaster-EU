{
  "buildCommand": "PORT=3000 BASE_PATH=/ pnpm --filter @workspace/steelcost-forecaster build && rm -rf public && cp -R artifacts/steelcost-forecaster/dist/public public",
  "outputDirectory": "public",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "vite"
}
