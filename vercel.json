{
  "buildCommand": "PORT=3000 BASE_PATH=/ pnpm --filter @workspace/steelcost-forecaster build && rm -rf public && cp -R dist/public public",
  "outputDirectory": "public",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "vite"
}
